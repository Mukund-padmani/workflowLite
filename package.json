{
    "name": "workflow-builder-lite",
    "version": "1.0.0",
    "description": "A workflow automation web application",
    "scripts": {
        "start": "node server/index.js",
        "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
        "dev": "concurrently \"npm run dev --prefix client\" \"npm start --prefix server\"",
        "client-install": "npm install --prefix client",
        "server-install": "npm install --prefix server",
        "postinstall": "npm run client-install && npm run server-install"
    },
    "keywords": [],
    "author": "workflowLite",
    "license": "ISC",
    "devDependencies": {
        "concurrently": "^8.2.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}